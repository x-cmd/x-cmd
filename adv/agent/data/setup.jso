{
"#tip:note"
:
[
{
"cn"
:
"setup 会完成以下配置：\n1. 创建 ~/AGENTS.md（如果不存在），注入 x-cmd 使用提示词\n2. 安装 skills 到 ~/.agents、~/.claude、~/.codex、~/.gemini、~/.config/opencode\n3. 配置各 AI 工具读取 ~/AGENTS.md\n运行一次即可，后续更新用 `x agent unify`\n"
,
"en"
:
"Setup performs:\n1. Create ~/AGENTS.md with x-cmd usage prompts\n2. Install skills to ~/.agents, ~/.claude, ~/.codex, ~/.gemini, ~/.config/opencode\n3. Configure AI tools to read ~/AGENTS.md\nRun once; use `x agent unify` for subsequent updates\n"
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x agent setup"
,
"cn"
:
"示例: 首次配置，自动设置所有 AI 工具"
,
"en"
:
"Example: First-time setup for all AI tools"
}
]
}

