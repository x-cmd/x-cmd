{
"#tip"
:
[
{
"cn"
:
"x agent init 是做什么的？\n它帮你选择并配置默认的 AI 工具（例如 Claude Code、Codex、Kimi CLI）。\n通常只需要运行一次；之后可以用 `x agent --cfg` 查看或修改默认工具。\n注意：init 不等于 setup。\n- init：选择默认 AI 工具，检查登录 / API key 状态。\n- setup：把 x-cmd 的提示词和技能安装到 AI 工具目录里。\n建议先运行 `x agent init`，再运行 `x agent setup`。\n想临时切换工具而不改默认配置？用 `x agent --cur set zero_harness=<工具名>`。"
,
"en"
:
"What does `x agent init` do?\nIt picks and configures your default AI tool (e.g. Claude Code, Codex, Kimi CLI).\nYou usually run it once; use `x agent --cfg` to view or change the default later.\nNote: init is not setup.\n- init: choose your default AI tool and verify login / API key.\n- setup: install x-cmd prompts and skills into the AI tool directories.\nRecommended: run `x agent init` first, then `x agent setup`.\nWant to temporarily switch tools without changing defaults? Use `x agent --cur set zero_harness=<tool-name>`."
}
,
{
"cn"
:
"支持的 AI 工具：\n- claude / claude-* — Claude Code 及其各平台变体\n- codex — OpenAI Codex\n- codex-ds — OpenAI Codex + DeepSeek model\n- cursor — Cursor Agent\n- gemini-cli — Google Gemini\n- opencode — 开源多模型工具\n- kimi-cli — Kimi 命令行工具\n"
,
"en"
:
"Supported AI tools:\n- claude / claude-* — Claude Code and provider variants\n- codex — OpenAI Codex\n- codex-ds — OpenAI Codex + DeepSeek model\n- cursor — Cursor Agent\n- gemini-cli — Google Gemini\n- opencode — Open-source multi-model tool\n- kimi-cli — Kimi CLI\n"
}
]
,
"#synopsis"
:
[
"x agent init"
,
"x agent init --full"
]
,
"#tldr"
:
[
{
"cmd"
:
"x agent init"
,
"cn"
:
"示例: 初始化配置，交互式选择默认 AI 工具（最小化模式）"
,
"en"
:
"Example: Initialize config, interactively select default AI tool (minimal mode)"
}
,
{
"cmd"
:
"x agent init --full"
,
"cn"
:
"示例: 完整配置模式，显示所有支持的 AI 工具"
,
"en"
:
"Example: Full configuration mode, show all supported AI tools"
}
]
}

