{
"#name"
:
{
"eget"
:
null
,
"en"
:
"Pull first-hand, ready-to-run binaries straight from the project's official repo, any version latest to legacy; plus bare-name one-step install — no sudo, with built-in hosted acceleration for the China region"
,
"cn"
:
"直连项目官仓取一手可运行二进制、新旧版本随取；新增裸名一键安装、免 sudo，为中国区内置托管加速"
}
,
"#desc"
:
{
"en"
:
"Download and install pre-built binaries from GitHub/GitLab/Codeberg Releases (Inspired by zyedidia/eget); `x eget use <name>` installs to ~/.local/bin in one step, and for well-known projects a bare name (e.g. `x eget use jq`) is auto-completed to the source repo via the x install database; supports GHPROXY for accelerating GitHub downloads; on slow downloads, automatically reroutes through x-cmd's eget hosting (keeps it working in the China region)."
,
"cn"
:
"从 GitHub/GitLab/Codeberg Release 下载并安装预编译二进制文件（Inspired by zyedidia/eget）；`x eget use <name>` 一键安装到 ~/.local/bin，对知名项目支持裸名（如 `x eget use jq`），由 x install 数据库补全源作者仓库；支持通过 GHPROXY 加速 GitHub 下载；下载遇网速问题时，自动改走 x-cmd 的 eget 加速托管（保证中国区可用）。"
}
,
"#tip"
:
[
{
"cn"
:
"纯 shell 重新实现，100% 兼容 zyedidia/eget CLI（inactive）。MIT License。\n"
,
"en"
:
"Pure shell reimplementation, 100% compatible with zyedidia/eget CLI (inactive). MIT License.\n"
}
,
{
"cn"
:
"推荐用法：`x eget use [owner/]<repo>` —— 从官方 Release 下载并安装到 ~/.local/bin（x-cmd 会自动把 ~/.local/bin 加入 PATH）。\n"
,
"en"
:
"Recommended: `x eget use [owner/]<repo>` — downloads from the official Release and installs to ~/.local/bin (x-cmd automatically adds ~/.local/bin to PATH).\n"
}
,
{
"cn"
:
"裸名补全（如 `x eget use jq` → jqlang/jq）的数据来自 x install 数据库。\n这个数据库只为方便把常用项目名映射到源作者仓库而存在，**不构成对项目本身的推荐或安全审查** —— 安装前请自行判断可信度。\n详见 https://x-cmd.com/install，或 `x i` 交互查看。\n如果你是项目作者，想让自己的项目进入数据库，可参考 https://x-cmd.com/install/submit。\n"
,
"en"
:
"Bare-name completion (e.g. `x eget use jq` → jqlang/jq) is powered by the x install database.\nThe database exists purely to map common project names to their source repos for convenience — it is **NOT a recommendation or security vetting** of the projects themselves. Judge trustworthiness before installing.\nSee https://x-cmd.com/install, or run `x i` for interactive browsing.\nIf you are the project author and want your project included in the database, see https://x-cmd.com/install/submit.\n"
}
,
{
"cn"
:
"多平台支持：GitHub、GitLab、Codeberg。短名 gh:/gl:/cb: 快速指定平台。\n例如 x eget gl:owner/repo 从 GitLab 下载。暂不支持 Gitee（Release 下载需登录）。\n"
,
"en"
:
"Multi-forge support: GitHub, GitLab, Codeberg. Use short names gh:/gl:/cb: to specify.\ne.g., x eget gl:owner/repo downloads from GitLab. Gitee not supported (requires login).\n"
}
,
{
"cn"
:
"x eget 直接从作者仓库下载，可按需获取所有版本。x pkg 是 x-cmd 官方打包维护的稳定发行渠道，只引入经过验证的稳定版本。\n"
,
"en"
:
"x eget downloads directly from the author's repo — all versions available on demand. x pkg is x-cmd's curated stable channel, only shipping vetted releases.\n"
}
,
{
"cn"
:
"开发者按规范打包并发布到 Release，用户通过 x eget 即下即用，通过 x eget snap 即下即运行。\n"
,
"en"
:
"Developers package and publish to releases following naming conventions, users download instantly with x eget and run instantly with x eget snap.\n"
}
,
{
"cn"
:
"下载遇到网速问题时（中国区尤为常见），x-cmd 会自动改走 eget 加速托管来完成下载 —— 这是 x-cmd 的一个特色，\n无需用户配置，任何网络环境下 `x eget` 都装得上。日志会写明切换原因（如 `stalled (connection too slow)`）；\n不需要可用 `--no-mirror` 关闭。\n"
,
"en"
:
"When a download hits speed issues (notably in the China region), x-cmd automatically reroutes through\nits eget hosting to finish the download — a feature, not a config, with nothing to set up, so `x eget`\njust works on any network. The switch reason is named in the log (e.g., `stalled (connection too slow)`);\ndisable with --no-mirror.\n"
}
,
{
"cn"
:
"不管是你自己的 ghproxy，还是上面的 eget 加速托管，都只作用于 asset 的下载；api.github.com 始终保持直连。\nGitHub 下载默认带 SHA-256 校验，digest 直接从 api.github.com 取得、不经过任何托管 —— 因此校验值始终可信，\n能用来验证托管下回来的 asset，保证下载的完整与安全。GitLab / Codeberg release API 无 digest 字段，这两条链路\n静默跳过校验；可用 --no-verify 关闭。\n"
,
"en"
:
"Whether you use your own ghproxy or x-cmd's eget hosting above, only the asset download is rerouted —\napi.github.com always stays direct. GitHub downloads are SHA-256 verified by default, with the digest\nfetched straight from api.github.com and never through any hosting, so the checksum stays trustworthy and\ncan verify the hosted asset, guaranteeing download integrity and security. GitLab / Codeberg release APIs\nhave no digest field, so those links skip verification silently; use --no-verify to disable.\n"
}
,
{
"cn"
:
"GitHub API 限流（60 req/hr/IP 未认证）的规避策略：先按 `EGET_GITHUB_TOKEN` → `GITHUB_TOKEN` → `GH_TOKEN` → `gh auth token` 顺序找 token\n（登录了 gh CLI 的用户无需额外配置，自动用 5000/hr 的认证额度）；如果实在没有 token、或者仍然被限流，就回退到抓 GitHub releases\n页面 HTML（`expanded_assets/<tag>`）来取 asset 链接 —— 这条路径几乎不限流。日常使用很少会撞上限流。\nCI / 合规场景如果不想 eget 隐式读取 `gh` 凭据，置 `EGET_NO_GH_AUTH_TOKEN=1` 即可关闭 `gh auth token` 自动查找，只用显式 env var。\n"
,
"en"
:
"How rate limits are avoided on api.github.com (60 req/hr unauthenticated). Token chain: `EGET_GITHUB_TOKEN` → `GITHUB_TOKEN`\n→ `GH_TOKEN` → `gh auth token` — users with a configured `gh` CLI get the 5000/hr authenticated quota automatically, no env var\nto export. If no token is available (or auth still rate-limits), we fall back to scraping the GitHub releases page HTML\n(`expanded_assets/<tag>`) for asset links — that path is barely throttled. In practice you almost never hit a limit.\nIn CI / compliance scenarios, set `EGET_NO_GH_AUTH_TOKEN=1` to disable the `gh auth token` auto-lookup — only explicit\nenv vars are honored.\n"
}
]
,
"#tip:note"
:
[
{
"cn"
:
"环境变量 `___X_CMD_GHPROXY_ENDPOINT`：GitHub 直连慢时走代理。仅作用于 github.com 资源下载，不影响 api.github.com 与其它 forge（gitlab / codeberg）。\n支持两种写法，自动识别：\n  - 前缀型：`___X_CMD_GHPROXY_ENDPOINT=\"https://your-proxy.example.com\"`         => https://your-proxy.example.com/https://github.com/...\n  - 替换型：`___X_CMD_GHPROXY_ENDPOINT=\"https://your-proxy.example.com/github.com\"` => https://your-proxy.example.com/github.com/...\n末尾斜杠两种模式都会自动去掉。设置后 `x eget` / `x eget snap` 的下载链路均生效；下载日志会打印 `Using GHPROXY: ...` 提示。\n"
,
"en"
:
"Env `___X_CMD_GHPROXY_ENDPOINT`: route slow GitHub downloads through a proxy. Applies only to github.com asset downloads — api.github.com and other forges (gitlab / codeberg) are unaffected.\nTwo endpoint shapes are auto-detected:\n  - prefix:  `___X_CMD_GHPROXY_ENDPOINT=\"https://your-proxy.example.com\"`         => https://your-proxy.example.com/https://github.com/...\n  - replace: `___X_CMD_GHPROXY_ENDPOINT=\"https://your-proxy.example.com/github.com\"` => https://your-proxy.example.com/github.com/...\nTrailing slashes are normalized. Once set, both `x eget` and `x eget snap` are affected; download log prints `Using GHPROXY: ...`.\n"
}
]
,
"#subcmd:download"
:
[
"download"
]
,
"#subcmd:snap"
:
[
"snap"
,
"which"
]
,
"#subcmd:use"
:
[
"use"
,
"unuse"
,
"usels"
,
"upgrade"
]
,
"#subcmd:query"
:
[
"ls"
,
"resolve"
,
"rate"
]
,
"#subcmd:util"
:
[
"extract"
]
,
"#subcmd:cache"
:
[
"cache"
]
,
"#synopsis"
:
[
{
"x eget [OPTIONS] [owner/]<repo>"
:
null
}
,
{
"x eget [OPTIONS] URL"
:
null
}
,
{
"x eget use [OPTIONS] [owner/]<repo>"
:
null
}
,
{
"x eget unuse <bin|owner/repo>"
:
null
}
,
{
"x eget unuse --all"
:
null
}
,
{
"x eget upgrade [OPTIONS] <name>"
:
null
}
,
{
"x eget upgrade --all"
:
null
}
,
{
"x eget snap [OPTIONS] <repo> [-- args...]"
:
null
}
,
{
"x eget usels"
:
null
}
,
{
"x eget which [owner/]<repo>"
:
null
}
,
{
"x eget ls [OPTIONS] <repo> [<release>]"
:
null
}
,
{
"x eget resolve [OPTIONS] <repo>"
:
null
}
,
{
"x eget extract [OPTIONS] <archive>"
:
null
}
,
{
"x eget rate"
:
null
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget use fzf"
,
"cn"
:
"安装到 ~/.local/bin（推荐用法）"
,
"en"
:
"Install to ~/.local/bin (recommended)"
}
,
{
"cmd"
:
"x eget unuse fzf"
,
"cn"
:
"移除 use 安装的 binary"
,
"en"
:
"Remove a use-installed binary"
}
,
{
"cmd"
:
"x eget snap fzf -- --version"
,
"cn"
:
"即下即运行，不安装到 PATH（类似 npx）"
,
"en"
:
"Download and run instantly, no PATH install (like npx)"
}
,
{
"cmd"
:
"x eget ls"
,
"cn"
:
"浏览 x install 收录的所有可 eget 安装的包（约 800+）"
,
"en"
:
"Browse all eget-installable packages indexed by x install (~800+)"
}
,
{
"cmd"
:
"x eget ls fzf"
,
"cn"
:
"列出某仓库的所有 release 版本"
,
"en"
:
"List all releases of a repo"
}
,
{
"cmd"
:
"x eget BurntSushi/ripgrep"
,
"cn"
:
"原始 eget — 下载默认 binary 到当前目录"
,
"en"
:
"Original eget: download default binary to current directory"
}
,
{
"cmd"
:
"x eget --to ~/.local/bin BurntSushi/ripgrep"
,
"cn"
:
"原始 eget — 下载到指定目录"
,
"en"
:
"Original eget: download to a specific directory"
}
,
{
"cmd"
:
"x eget --tag v2.42.1 cli/cli"
,
"cn"
:
"原始 eget — 下载指定版本"
,
"en"
:
"Original eget: download a specific version"
}
,
{
"cmd"
:
"x eget --system linux/amd64 cli/cli"
,
"cn"
:
"原始 eget — 交叉下载其他平台"
,
"en"
:
"Original eget: cross-platform download"
}
,
{
"cmd"
:
"x eget https://github.com/x-cmd-build/rumdl/releases/tag/0.1.25"
,
"cn"
:
"原始 eget — 通过 URL 下载"
,
"en"
:
"Original eget: download via URL"
}
,
{
"cmd"
:
"x eget gl:owner/repo"
,
"cn"
:
"原始 eget — 使用短名从 GitLab 下载"
,
"en"
:
"Original eget: download from GitLab using short name"
}
]
,
"download"
:
{
"#desc"
:
{
"cn"
:
"下载并安装 binary —— 这是 x eget 的默认行为，可省略子命令直接 `x eget <repo>`"
,
"en"
:
"Download and install binary — the default behavior; the subcmd can be omitted (`x eget <repo>`)"
}
,
"#synopsis"
:
[
{
"x eget [OPTIONS] [owner/]<repo>"
:
null
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget BurntSushi/ripgrep"
,
"cn"
:
"下载默认 binary 到当前目录"
,
"en"
:
"Download the default binary to the current directory"
}
,
{
"cmd"
:
"x eget --to ~/.local/bin BurntSushi/ripgrep"
,
"cn"
:
"下载到指定目录"
,
"en"
:
"Download to a specific directory"
}
,
{
"cmd"
:
"x eget --tag v2.42.1 cli/cli"
,
"cn"
:
"下载指定版本"
,
"en"
:
"Download a specific version"
}
,
{
"cmd"
:
"x eget --system linux/amd64 cli/cli"
,
"cn"
:
"交叉下载其他平台"
,
"en"
:
"Cross-platform download"
}
,
{
"cmd"
:
"x eget --download-only cli/cli"
,
"cn"
:
"仅下载到 tmp，不解压安装"
,
"en"
:
"Download only, skip extraction"
}
]
,
"--to"
:
{
"#desc"
:
{
"cn"
:
"安装目录（默认当前目录）"
,
"en"
:
"Install directory (default: current directory)"
}
,
"#1"
:
{
"#desc"
:
{
"cn"
:
"目录路径"
,
"en"
:
"Directory path"
}
}
}
,
"-t|--tag"
:
{
"#desc"
:
{
"cn"
:
"指定版本标签"
,
"en"
:
"Specify version tag"
}
,
"#1"
:
{
"#desc"
:
{
"cn"
:
"版本标签"
,
"en"
:
"Version tag"
}
}
}
,
"-s|--system"
:
{
"#desc"
:
{
"cn"
:
"指定目标平台（如 linux/amd64、darwin/arm64）"
,
"en"
:
"Specify target platform (e.g., linux/amd64, darwin/arm64)"
}
,
"#1"
:
{
"#desc"
:
{
"cn"
:
"平台标识"
,
"en"
:
"Platform identifier"
}
}
}
,
"--download-only"
:
{
"#desc"
:
{
"cn"
:
"仅下载压缩包到 tmp，不解压"
,
"en"
:
"Download the archive to tmp only, no extraction"
}
}
,
"--source"
:
{
"#desc"
:
{
"cn"
:
"下载源码 tarball 而非预编译 binary"
,
"en"
:
"Download source tarball instead of pre-built binary"
}
}
,
"--pre-release"
:
{
"#desc"
:
{
"cn"
:
"允许预发布版本"
,
"en"
:
"Allow pre-release versions"
}
}
,
"--forge"
:
{
"#desc"
:
{
"cn"
:
"指定 forge（github / gitlab / codeberg）"
,
"en"
:
"Specify forge (github / gitlab / codeberg)"
}
}
,
"-q|--quiet"
:
{
"#desc"
:
{
"cn"
:
"静默模式"
,
"en"
:
"Quiet mode"
}
}
,
"--no-verify"
:
{
"#desc"
:
{
"cn"
:
"跳过 SHA-256 校验"
,
"en"
:
"Skip SHA-256 verification"
}
}
,
"--speed-limit"
:
{
"#desc"
:
{
"cn"
:
"速度兜底阈值（字节/秒），低于此速度持续 N 秒则断开走 mirror（默认 50000）"
,
"en"
:
"Stall threshold (bytes/sec); below this for N seconds, abort and retry via mirror (default 50000)"
}
,
"#1"
:
{
"#desc"
:
{
"cn"
:
"字节/秒阈值"
,
"en"
:
"Bytes-per-second threshold"
}
}
}
,
"--speed-time"
:
{
"#desc"
:
{
"cn"
:
"持续低速多少秒后断开走 mirror（默认 5）"
,
"en"
:
"Seconds of sustained slow speed before aborting to mirror (default 5)"
}
,
"#1"
:
{
"#desc"
:
{
"cn"
:
"秒数"
,
"en"
:
"Seconds"
}
}
}
,
"--no-mirror"
:
{
"#desc"
:
{
"cn"
:
"禁用 eget 加速托管，下载全程直连"
,
"en"
:
"Disable eget hosting; download directly throughout"
}
}
}
,
"snap"
:
{
"#desc"
:
{
"cn"
:
"即下即运行，不安装到 PATH；类似 npx"
,
"en"
:
"Download and run instantly without PATH install; like npx"
}
,
"#synopsis"
:
[
{
"x eget snap [OPTIONS] <repo> [-- args...]"
:
null
}
]
,
"#tip"
:
[
{
"cn"
:
"首次下载后缓存到本地，后续调用零网络开销。\n"
,
"en"
:
"First run downloads and caches locally; subsequent runs have zero network overhead.\n"
}
,
{
"cn"
:
"--auto-check-update 1d：超过 1 天自动检查 GitHub Release 是否有新版本。间隔最小 5m；检查失败后同样至少等 5 分钟再重试。\n"
,
"en"
:
"--auto-check-update 1d: auto-check GitHub Release for new versions after 1 day. Minimum interval 5m; on failure, same 5m cooldown before retry.\n"
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget snap BurntSushi/ripgrep --version"
,
"cn"
:
"直接运行 ripgrep"
,
"en"
:
"Run ripgrep directly"
}
,
{
"cmd"
:
"x eget snap sharkdp/fd -e py -e js ."
,
"cn"
:
"按扩展名搜索文件"
,
"en"
:
"Search files by extension"
}
,
{
"cmd"
:
"x eget snap junegunn/fzf --tmux"
,
"cn"
:
"在 tmux 中运行 fzf"
,
"en"
:
"Run fzf inside tmux"
}
,
{
"cmd"
:
"x eget snap --auto-check-update 1d BurntSushi/ripgrep --version"
,
"cn"
:
"每天自动检查更新"
,
"en"
:
"Auto-check for updates daily"
}
,
{
"cmd"
:
"x eget snap --tag 14.1.0 BurntSushi/ripgrep -- -V"
,
"cn"
:
"运行指定版本"
,
"en"
:
"Run a specific version"
}
,
{
"cmd"
:
"x eget snap --update BurntSushi/ripgrep --version"
,
"cn"
:
"强制检查更新"
,
"en"
:
"Force check for updates"
}
]
,
"--auto-check-update"
:
{
"#desc"
:
{
"cn"
:
"定期检查 GitHub Release 新版本（如 1d、1h、5m，最小 5m）"
,
"en"
:
"Periodically check GitHub Release for new versions (e.g. 1d, 1h, 5m, minimum 5m)"
}
,
"#1"
:
{
"#desc"
:
{
"cn"
:
"检查间隔（如 1d、1h、5m）"
,
"en"
:
"Check interval (e.g. 1d, 1h, 5m)"
}
}
}
,
"--update"
:
{
"#desc"
:
{
"cn"
:
"强制检查更新，忽略缓存"
,
"en"
:
"Force check for updates, ignore cache"
}
}
,
"-t|--tag"
:
{
"#desc"
:
{
"cn"
:
"指定版本标签"
,
"en"
:
"Specify version tag"
}
}
,
"-s|--system"
:
{
"#desc"
:
{
"cn"
:
"指定目标平台（如 linux/amd64）"
,
"en"
:
"Specify target platform (e.g. linux/amd64)"
}
}
,
"-q|--quiet"
:
{
"#desc"
:
{
"cn"
:
"静默模式"
,
"en"
:
"Quiet mode"
}
}
}
,
"use"
:
{
"#desc"
:
{
"cn"
:
"安装 binary 到 ~/.local/bin"
,
"en"
:
"Install binary to ~/.local/bin"
}
,
"#synopsis"
:
[
{
"x eget use [OPTIONS] <repo>"
:
null
}
]
,
"#tip"
:
[
{
"cn"
:
"无需先 snap，use 会自动下载并安装。如果已有 snap 缓存则直接使用。\n"
,
"en"
:
"No need to snap first — use auto-downloads and installs. Uses snap cache if available.\n"
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget use jq"
,
"cn"
:
"用裸名安装（自动反查 jqlang/jq）"
,
"en"
:
"Install by bare name (auto-resolves to jqlang/jq)"
}
,
{
"cmd"
:
"x eget use BurntSushi/ripgrep"
,
"cn"
:
"用 owner/repo 安装"
,
"en"
:
"Install by owner/repo"
}
,
{
"cmd"
:
"x eget use --tag 0.55.0 junegunn/fzf"
,
"cn"
:
"安装指定版本"
,
"en"
:
"Install a specific version"
}
,
{
"cmd"
:
"x eget use --to /usr/local/bin cli/cli"
,
"cn"
:
"安装到指定目录"
,
"en"
:
"Install to a custom directory"
}
,
{
"cmd"
:
"x eget use --force fzf"
,
"cn"
:
"覆盖已存在的同名 binary"
,
"en"
:
"Overwrite an existing binary"
}
]
,
"--to"
:
{
"#desc"
:
{
"cn"
:
"目标目录（默认 ~/.local/bin）"
,
"en"
:
"Target directory (default ~/.local/bin)"
}
,
"#1"
:
{
"#desc"
:
{
"cn"
:
"目录路径"
,
"en"
:
"Directory path"
}
}
}
,
"-t|--tag"
:
{
"#desc"
:
{
"cn"
:
"指定 release 版本标签"
,
"en"
:
"Specify a release version tag"
}
,
"#1"
:
{
"#desc"
:
{
"cn"
:
"版本标签（如 v1.0.0、0.55.0）"
,
"en"
:
"Version tag (e.g., v1.0.0, 0.55.0)"
}
}
}
,
"--force"
:
{
"#desc"
:
{
"cn"
:
"覆盖已存在的文件"
,
"en"
:
"Overwrite existing file"
}
}
}
,
"unuse"
:
{
"#desc"
:
{
"cn"
:
"移除 use 安装的 binary"
,
"en"
:
"Remove a use-installed binary"
}
,
"#synopsis"
:
[
{
"x eget unuse <bin|owner/repo>"
:
null
}
,
{
"x eget unuse --all"
:
null
}
]
,
"--all"
:
{
"#desc"
:
{
"cn"
:
"移除所有 use 安装的 binary"
,
"en"
:
"Remove every use-installed binary"
}
}
,
"#tldr"
:
[
{
"cmd"
:
"x eget unuse rg"
,
"cn"
:
"移除 rg"
,
"en"
:
"Remove rg"
}
,
{
"cmd"
:
"x eget unuse BurntSushi/ripgrep"
,
"cn"
:
"按全名移除"
,
"en"
:
"Remove by owner/repo"
}
,
{
"cmd"
:
"x eget unuse --all"
,
"cn"
:
"移除所有"
,
"en"
:
"Remove all"
}
]
}
,
"usels"
:
{
"#desc"
:
{
"cn"
:
"列出所有 use 安装的记录（owner/repo 与 binary 名），TSV 格式带表头"
,
"en"
:
"List all use-installed records (owner/repo and binary name), TSV with header"
}
,
"#tldr"
:
[
{
"cmd"
:
"x eget usels"
,
"cn"
:
"列出全部安装记录"
,
"en"
:
"List all installation records"
}
,
{
"cmd"
:
"x eget usels | tail -n +2 | wc -l"
,
"cn"
:
"统计安装条数（跳过表头）"
,
"en"
:
"Count installations (skip header)"
}
,
{
"cmd"
:
"x eget usels | grep ripgrep"
,
"cn"
:
"过滤特定仓库"
,
"en"
:
"Filter to a specific repo"
}
]
}
,
"upgrade"
:
{
"#desc"
:
{
"cn"
:
"把 use 装的 binary 刷新到最新 release（默认走 snap cache，重复 use 是不会自动升级的）"
,
"en"
:
"Refresh use-installed binaries to their latest release (snap cache means repeated 'use' won't auto-upgrade)"
}
,
"#synopsis"
:
[
{
"x eget upgrade <name>"
:
null
}
,
{
"x eget upgrade --all"
:
null
}
,
{
"x eget upgrade --dry-run [--all | <name>]"
:
null
}
]
,
"#tip"
:
[
{
"cn"
:
"`x eget use <name>` 不会自动检查升级 —— 首次安装后，重复调用永远走 snap cache，0 网络。\n想拿到最新版本必须显式调用 `x eget upgrade`。\n裸 `x eget upgrade`（无参数、无 --all）会报错以避免误触 —— 要么指定名字，要么显式 --all。\n`update` 是 `upgrade` 的别名（eget 没有 package index，二者语义无区别）。\n"
,
"en"
:
"`x eget use <name>` does NOT auto-check for upgrades — after the first install, repeated calls always hit the snap cache with zero network.\nTo get the latest release you must explicitly run `x eget upgrade`.\nBare `x eget upgrade` (no args, no --all) errors out to avoid footguns — pass either a name or --all.\n`update` is an alias for `upgrade` (eget has no package index, so the two are synonymous).\n"
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget upgrade --dry-run --all"
,
"cn"
:
"预览所有可升级的 binary（不改文件）"
,
"en"
:
"Preview all upgradable binaries (no filesystem changes)"
}
,
{
"cmd"
:
"x eget upgrade --all"
,
"cn"
:
"升级所有 use 装的 binary"
,
"en"
:
"Upgrade every use-tracked binary"
}
,
{
"cmd"
:
"x eget upgrade fzf"
,
"cn"
:
"只升级匹配名字或 owner/repo 的 binary"
,
"en"
:
"Upgrade only entries matching by binname or owner/repo"
}
]
,
"#1"
:
{
"#desc"
:
{
"cn"
:
"名字或 owner/repo（按 binname 精确匹配，否则按 owner/repo 子串匹配）"
,
"en"
:
"Name or owner/repo (exact match on binname, otherwise substring match on owner/repo)"
}
}
,
"--all"
:
{
"#desc"
:
{
"cn"
:
"升级 use 跟踪的所有 binary"
,
"en"
:
"Upgrade every use-tracked binary"
}
}
,
"--dry-run"
:
{
"#desc"
:
{
"cn"
:
"只打印 current→latest 对比，不下载、不替换文件"
,
"en"
:
"Print current→latest comparison only; do not download or replace files"
}
}
}
,
"which"
:
{
"#desc"
:
{
"cn"
:
"查找 snap 缓存的 binary 完整路径（不解压、不下载，仅返回已缓存版本）"
,
"en"
:
"Find the full path of a snap-cached binary (no extract, no download — just returns the cached path)"
}
,
"#synopsis"
:
[
{
"x eget which [owner/]<repo>"
:
null
}
,
{
"x eget which [owner/]<repo>"
:
null
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget which BurntSushi/ripgrep"
,
"cn"
:
"按 owner/repo 查找缓存路径"
,
"en"
:
"Look up cache path by owner/repo"
}
,
{
"cmd"
:
"x eget which rg"
,
"cn"
:
"按裸名查找（自动通过 x install 反查 owner）"
,
"en"
:
"Look up by bare name (owner auto-resolved via x install)"
}
,
{
"cmd"
:
"x eget which gh:cli/cli"
,
"cn"
:
"用短名指定 forge"
,
"en"
:
"Specify forge with short prefix"
}
,
{
"cmd"
:
"x eget which BurntSushi/ripgrep"
,
"cn"
:
"按 owner/repo 查找缓存路径"
,
"en"
:
"Look up cache path by owner/repo"
}
,
{
"cmd"
:
"x eget which rg"
,
"cn"
:
"按裸名查找（自动通过 x install 反查 owner）"
,
"en"
:
"Look up by bare name (owner auto-resolved via x install)"
}
,
{
"cmd"
:
"x eget which gh:cli/cli"
,
"cn"
:
"用短名指定 forge"
,
"en"
:
"Specify forge with short prefix"
}
]
}
,
"ls"
:
{
"#desc"
:
{
"cn"
:
"不带参数时，列出 x install 数据库中所有可用 eget 安装的包；带参数时，列出仓库的 release 版本或指定版本的 artifact"
,
"en"
:
"Without args, list all eget-installable packages from the x install database; with a repo, list its releases or a specific release's artifacts"
}
,
"#synopsis"
:
[
{
"x eget ls [OPTIONS]"
:
null
}
,
{
"x eget ls [OPTIONS] <repo> [<release>]"
:
null
}
]
,
"#tip"
:
[
{
"cn"
:
"无参数调用 `x eget ls` 会从 `x install --ls --tsv` 中筛选 rule 列含 `/eget` 的条目，输出 NAME / REPO / DESCRIPTION 三列 TSV。\n数据量较大（约 800+ 行），适合管道处理：`x eget ls | grep ripgrep`、`x eget ls | head`、`x eget ls | fzf`。\n"
,
"en"
:
"Calling `x eget ls` with no args filters the x install TSV for rows whose rule column contains `/eget`, emitting a 3-column TSV: NAME / REPO / DESCRIPTION.\nThe list is large (~800 entries) and pipe-friendly: `x eget ls | grep ripgrep`, `x eget ls | head`, `x eget ls | fzf`.\n"
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget ls"
,
"cn"
:
"列出所有可用 eget 安装的包（来自 x install）"
,
"en"
:
"List all eget-installable packages (from x install)"
}
,
{
"cmd"
:
"x eget ls | grep ripgrep"
,
"cn"
:
"过滤特定关键词"
,
"en"
:
"Filter by keyword"
}
,
{
"cmd"
:
"x eget ls BurntSushi/ripgrep"
,
"cn"
:
"列出仓库的所有 release 版本"
,
"en"
:
"List all releases of a repo"
}
,
{
"cmd"
:
"x eget ls BurntSushi/ripgrep 14.1.0"
,
"cn"
:
"列出指定 release 的 artifact"
,
"en"
:
"List artifacts in a specific release"
}
,
{
"cmd"
:
"x eget ls gl:owner/repo"
,
"cn"
:
"列出 GitLab 仓库的 release"
,
"en"
:
"List GitLab releases"
}
]
,
"#1"
:
{
"#desc"
:
{
"cn"
:
"仓库（user/repo 或 gh:/gl:/cb: 短名）"
,
"en"
:
"Repository (user/repo or gh:/gl:/cb: short name)"
}
}
,
"#2"
:
{
"#desc"
:
{
"cn"
:
"指定版本标签（省略则列出所有版本）"
,
"en"
:
"Release tag (omit to list all releases)"
}
}
,
"--forge"
:
{
"#desc"
:
{
"cn"
:
"指定平台"
,
"en"
:
"Specify forge"
}
}
,
"--limit"
:
{
"#desc"
:
{
"cn"
:
"最大列出版本数（默认 20）"
,
"en"
:
"Max releases to list (default 20)"
}
}
,
"--no-ccmd"
:
{
"#desc"
:
{
"cn"
:
"跳过缓存，强制重新请求 API"
,
"en"
:
"Skip cache, force fresh API request"
}
}
}
,
"resolve"
:
{
"#desc"
:
{
"cn"
:
"解析匹配结果，不下载（显示选中的 artifact、URL、版本）"
,
"en"
:
"Resolve best matching asset without downloading"
}
,
"#tldr"
:
[
{
"cmd"
:
"x eget resolve BurntSushi/ripgrep"
,
"cn"
:
"预览匹配结果"
,
"en"
:
"Preview match result"
}
,
{
"cmd"
:
"x eget resolve --system linux/amd64 cli/cli"
,
"cn"
:
"预览指定平台的匹配"
,
"en"
:
"Preview match for a specific platform"
}
]
}
,
"extract"
:
{
"#desc"
:
{
"cn"
:
"从本地压缩包中提取 binary"
,
"en"
:
"Extract binary from a local archive"
}
,
"#synopsis"
:
[
{
"x eget extract [OPTIONS] <archive>"
:
null
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget extract ./rg-14.1.0.tar.gz"
,
"cn"
:
"解压到当前目录"
,
"en"
:
"Extract to the current directory"
}
,
{
"cmd"
:
"x eget extract --to /usr/local/bin ./rg-14.1.0.tar.gz"
,
"cn"
:
"解压到指定目录"
,
"en"
:
"Extract to a specific directory"
}
,
{
"cmd"
:
"x eget extract -f 'rg*' ./archive.tar.gz"
,
"cn"
:
"按文件名 glob 提取某个文件"
,
"en"
:
"Extract a specific file via name glob"
}
,
{
"cmd"
:
"x eget extract --all ./archive.tar.gz"
,
"cn"
:
"解压全部内容"
,
"en"
:
"Extract everything in the archive"
}
]
}
,
"cache"
:
{
"#desc"
:
{
"cn"
:
"统一管理 eget 缓存（原始归档 + 抽取后的 snap binary）"
,
"en"
:
"Unified management of eget caches (raw archives + extracted snap binaries)"
}
,
"#synopsis"
:
[
{
"x eget cache [OPERATION]"
:
null
}
]
,
"#tip"
:
[
{
"cn"
:
"eget 维护两套缓存：\n  - `cache/<owner>--<repo>/<version>/<asset>` —— 原始下载的归档包，带 SHA-256 校验。\n  - `snap/<owner>--<repo>/<version>/bin/<binary>` —— 抽取后可直接执行的 binary。\n`snap` / `use` / `upgrade` 全部走 snap 缓存；`download` 默认归档。\n"
,
"en"
:
"eget keeps two caches:\n  - `cache/<owner>--<repo>/<version>/<asset>` — raw downloaded archive, SHA-256 verified.\n  - `snap/<owner>--<repo>/<version>/bin/<binary>` — extracted ready-to-run binary.\n`snap` / `use` / `upgrade` all share the snap cache; `download` defaults to the archive cache.\n"
}
,
{
"cn"
:
"`clear` 对两类缓存采用不同策略：归档每仓库保留 N 个版本（默认 5）；snap 只保留 `.default-version`（当前 active 版本），其它版本视为过期。\nsnap 默认版本被 `clear` 保护 —— 想彻底清掉需要 `rm <owner/repo>` 或 `purge`。\n"
,
"en"
:
"`clear` uses different policies: archive keeps N versions per repo (default 5); snap keeps only `.default-version` (the active binary).\nThe snap default version is protected by `clear` — to wipe it, use `rm <owner/repo>` or `purge`.\n"
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget cache ls"
,
"cn"
:
"列出所有缓存条目（带 KIND / REPO / VERSION / BYTES 列）"
,
"en"
:
"List all cache entries (with KIND / REPO / VERSION / BYTES columns)"
}
,
{
"cmd"
:
"x eget cache info"
,
"cn"
:
"显示三类缓存（归档 / snap / use 列表）的路径与占用"
,
"en"
:
"Show path and size of the three caches (archive / snap / use list)"
}
,
{
"cmd"
:
"x eget cache clear 5"
,
"cn"
:
"清理：归档每仓库保留 5 个版本，snap 仅保留 .default-version"
,
"en"
:
"Clear: archive keeps 5 versions per repo; snap keeps .default-version only"
}
,
{
"cmd"
:
"x eget cache rm jqlang/jq"
,
"cn"
:
"删除某仓库的归档 + snap 缓存"
,
"en"
:
"Remove a repo's archive + snap cache"
}
,
{
"cmd"
:
"x eget cache rm https://github.com/jqlang/jq"
,
"cn"
:
"用 URL 删除（与 owner/repo 等价）"
,
"en"
:
"Remove by URL (equivalent to owner/repo)"
}
,
{
"cmd"
:
"x eget cache purge"
,
"cn"
:
"全部清空（不可恢复）"
,
"en"
:
"Purge everything (irreversible)"
}
]
,
"ls"
:
{
"#desc"
:
{
"cn"
:
"列出 archive + snap 缓存条目（TSV：KIND / REPO / VERSION / BYTES / PATH）"
,
"en"
:
"List archive + snap cache entries (TSV: KIND / REPO / VERSION / BYTES / PATH)"
}
,
"#synopsis"
:
[
{
"x eget cache ls"
:
null
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget cache ls"
,
"cn"
:
"列出所有缓存条目"
,
"en"
:
"List all cache entries"
}
,
{
"cmd"
:
"x eget cache ls | head"
,
"cn"
:
"只看前面几条"
,
"en"
:
"Show first few entries"
}
]
}
,
"info"
:
{
"#desc"
:
{
"cn"
:
"显示三类缓存的路径与大小"
,
"en"
:
"Show path and size of the three caches"
}
,
"#synopsis"
:
[
{
"x eget cache info"
:
null
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget cache info"
,
"cn"
:
"显示 archive / snap / use 列表的路径与占用"
,
"en"
:
"Show path and size of archive / snap / use list"
}
]
}
,
"clear"
:
{
"#desc"
:
{
"cn"
:
"清理旧版本（归档每仓库保留 N 个，默认 5；snap 保留 .default-version）"
,
"en"
:
"Prune old versions (archive keeps N per repo, default 5; snap keeps .default-version)"
}
,
"#synopsis"
:
[
{
"x eget cache clear [N]"
:
null
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget cache clear 5"
,
"cn"
:
"清理：归档每仓库保留 5 个版本，snap 仅保留 .default-version"
,
"en"
:
"Clear: archive keeps 5 versions per repo; snap keeps .default-version only"
}
,
{
"cmd"
:
"x eget cache clear 0"
,
"cn"
:
"移除所有归档，snap 仅保留 .default-version"
,
"en"
:
"Remove all archives, snap keeps .default-version only"
}
]
}
,
"purge"
:
{
"#desc"
:
{
"cn"
:
"清空全部缓存（不可恢复）"
,
"en"
:
"Wipe both caches (irreversible)"
}
,
"#synopsis"
:
[
{
"x eget cache purge"
:
null
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget cache purge"
,
"cn"
:
"全部清空（不可恢复，下次 use/snap 会重新下载）"
,
"en"
:
"Purge everything (irreversible; next use/snap re-downloads)"
}
]
}
,
"rm"
:
{
"#desc"
:
{
"cn"
:
"删除某仓库的归档 + snap 缓存"
,
"en"
:
"Remove a single repo from both caches"
}
,
"#synopsis"
:
[
{
"x eget cache rm <target>"
:
null
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget cache rm jqlang/jq"
,
"cn"
:
"用 owner/repo 删除"
,
"en"
:
"Remove by owner/repo"
}
,
{
"cmd"
:
"x eget cache rm https://github.com/jqlang/jq"
,
"cn"
:
"用 URL 删除（与 owner/repo 等价）"
,
"en"
:
"Remove by URL (equivalent to owner/repo)"
}
]
}
}
,
"rate"
:
{
"#desc"
:
{
"cn"
:
"查看 GitHub API 速率限制"
,
"en"
:
"Show GitHub API rate limit"
}
,
"#synopsis"
:
[
{
"x eget rate"
:
null
}
]
,
"#tip"
:
[
{
"cn"
:
"目前只支持 GitHub —— api.github.com 提供 /rate_limit 端点返回剩余配额。\nGitLab / Codeberg 只在每个 API 响应头里报告 rate limit，没有可独立查询的端点，因此暂不支持。\n速率数据基于 EGET_GITHUB_TOKEN / GITHUB_TOKEN 鉴权。\n"
,
"en"
:
"Only GitHub is supported — api.github.com provides a /rate_limit endpoint returning remaining quota.\nGitLab / Codeberg only report rate limit via per-response HTTP headers, with no standalone query endpoint, so they are unsupported.\nAuthenticated via EGET_GITHUB_TOKEN / GITHUB_TOKEN.\n"
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget rate"
,
"cn"
:
"查看 GitHub API 速率限制（剩余 / 上限 / 重置时间）"
,
"en"
:
"Show GitHub API rate limit (remaining / limit / reset time)"
}
,
{
"cmd"
:
"x eget rate gh"
,
"cn"
:
"短名等价于默认 github"
,
"en"
:
"Short name equivalent to the default github"
}
]
}
,
"html"
:
{
"#desc"
:
{
"cn"
:
"通过 GitHub releases 页面 HTML 抓取 release 信息（API 限流时的降级路径）"
,
"en"
:
"Scrape the GitHub releases page HTML for release info (fallback when the API is rate-limited)"
}
,
"#synopsis"
:
[
{
"x eget html [OPTIONS] <owner>/<repo>"
:
null
}
]
,
"#tip"
:
[
{
"cn"
:
"当 api.github.com 被限流（60 req/hr/IP 未认证配额耗尽）时，download / use / snap 路径会自动回退到这条 HTML 抓取路径。\n`x eget html <repo>` 直接调用抓取器，不经 API —— 用来诊断「限流时 eget 实际看到什么」很方便。\n等价于社区里常用的 `curl .../releases/expanded_assets/<tag> | grep <repo> | grep download`，但按 GitHub releases 页面结构锚定了 owner/repo，避免误匹配。\n输出与 API 路径同结构的 JSON（`{tag_name, assets[]}`），digest 留空、size 为 0 —— 因为 HTML 页面不提供这些字段。\n"
,
"en"
:
"When api.github.com is rate-limited (60 req/hr unauthenticated quota exhausted), the download / use / snap paths automatically fall back to this HTML scraping path.\n`x eget html <repo>` invokes the scraper directly without going through the API — handy for diagnosing what eget actually sees when rate-limited.\nMirrors the community's `curl .../releases/expanded_assets/<tag> | grep <repo> | grep download`, but anchors owner/repo into the pattern to avoid wrong matches on multi-repo pages.\nOutput is a JSON object in the same `{tag_name, assets[]}` shape as the API path; digest is empty and size is 0 (the HTML page doesn't expose them).\n"
}
]
,
"#tldr"
:
[
{
"cmd"
:
"x eget html ljh-sh/zhhz"
,
"cn"
:
"抓取最新 release（不经 API）"
,
"en"
:
"Scrape the latest release (no API)"
}
,
{
"cmd"
:
"x eget html --tag v0.7.7 ljh-sh/zhhz"
,
"cn"
:
"抓取指定 tag 的 release"
,
"en"
:
"Scrape a specific tag"
}
]
,
"-t|--tag"
:
{
"#desc"
:
{
"cn"
:
"指定 release 版本标签"
,
"en"
:
"Specify release version tag"
}
,
"#1"
:
{
"#desc"
:
{
"cn"
:
"版本标签"
,
"en"
:
"Version tag"
}
}
}
}
}

