

# x claude session import XXX.tar.gz
# x claude session import -p . XXX.tar.gz

___x_cmd_claude_session_import(){
    [ $# -gt 0 ]    ||      set -- "$PWD"

    # local session=""
    local folder="$PWD"
    local project=""
    local input=""

    local op="$1";          shift
    while [ $# -gt 0 ]; do
        case "$1" in
            -p|--project)   project="$2";   N=claude M="Please provide argument after $1" arg:2:shift ;;

            # TODO:
            # -s|--session)   session="$2";   N=claude M="Please provide argument after $1" arg:2:shift ;;

            -i|--input)     output="$2";    N=claude M="Please provide argument after $1" arg:2:shift ;;

            # rewrite all of the folder information in the file ...
            # --rewrite)      ;;

            *)              break ;;
        esac
    done

    [ -z "$project" ]       ||  ___x_cmd_claude_session_pickupsession "$1" ;;

    local input="$1";       [ -f "$input" ]  ||  N=claude M="Please provide valid backup filepath -> $input"

    # unzip
    # move the relative folder ...
    # rewrite if necessary ...

}

___x_cmd_claude_session_pickupsession(){
    :
    # use the first project name ...

    # TODO: support all projects ...
}


