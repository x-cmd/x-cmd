
ccal-meta-010:
  name: ensure-all-metadata-is-right

  desc:
  -
  -

  # running x-test using (bash/zsh/dash with mawk,awk,gawk)
  x-test:
    testcase1: |
      # loading x-cmd
      x ccal info 260501

    testcase2: |
      # loading x-cmd

  x-skill-test:
    testcase1: |
      # ...
      # (1)
      # (2)
      # (3)
