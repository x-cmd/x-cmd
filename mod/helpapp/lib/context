# shellcheck shell=dash

___x_cmd_helpapp_context(){
    local cmdid="$1"
    [ -n "$cmdid" ] || {
        N=help M="Please provide command id" log:ret:64
        return 1
    }

    shift; eval set -- "$cmdid" "$@"
    ___x_cmd_helpapp_gen_help_mod "$@"
}
