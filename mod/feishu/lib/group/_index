# shellcheck shell=sh disable=SC2154,SC2120,SC3043,SC2034,SC2329

# Reference: https://open.feishu.cn/document/server-docs/group/chat/list

xrc:mod:lib feishu   group/ls

___x_cmd_feishu_group(){
    param:void

    [ $# -gt 0 ]    ||  set -- --help

    local op="$1" ;     shift
    case "$op" in
        -h|--help)  ___x_cmd help -m feishu group ; return 0 ;;
        ls)         ___x_cmd_feishu_group_"$op" "$@" ;;
        *)          N=feishu M="Unknown op -> $op" log:ret:64   ;;
    esac
}
