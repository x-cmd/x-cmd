
# TODO: Try proxy if blocked.
# x proxy manager -- multiple proxy pool. change if necessary.  # Let AI desicde which proxy to go.

# x proxy pool tping    # check all of the proxy pool is ready.
# x proxy pool ls
# x proxy pool add <name> <url>
# x proxy pool rm

# x proxy use <name>        # auto detech
# x proxy try|set <name>

# In the SKILL.md and help. Let's the AI Notice some softeware cannot works with . So use proxychains IP fetch by 'x proxy pool get <proxy-name>'

___x_cmd_proxy_pool(){
    :
}


