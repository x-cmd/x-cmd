
___x_cmd_cfgy_inner_unset(){
    local prefix="$1"
    if ___x_cmd_cfgy_obj_isloaded "$prefix"; then
        eval eval "\${${prefix}_unset}"
        eval unset "${prefix}_unset"
        return
    fi
    return 1
}
