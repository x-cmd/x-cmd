
___x_cmd_dos2unix___mac2dos(){
    while [ $# -gt 0 ]; do
        case "$1" in
            -h|--help)      ___x_cmd help -m dos2unix --dos2mac ; return ;;
        esac
        break
    done

    local input="${1:-/dev/stdin}"
    local output="${2:-/dev/stdout}"
    {
        ___x_cmd_dos2unix___mac2unix | ___x_cmd_dos2unix___unix2dos
    } <"$input" >"$output"
}
