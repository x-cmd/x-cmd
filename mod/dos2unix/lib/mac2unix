
___x_cmd_dos2unix___mac2unix(){
    if ___x_cmd_hascmd mac2unix; then
        ___x_cmd_cmds mac2unix "$@"
    else
        local pkg=dos2unix
        ! ___x_cmd os is win || pkg=m2-"$pkg"
        ___x_cmd pixi exec -s "$pkg" mac2unix "$@"
    fi
}
