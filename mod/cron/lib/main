# shellcheck shell=dash

xrc:mod:lib     cron   __runcmd

___x_cmd_cron___main(){
    [ "$#" -gt 0 ] ||   set -- --help

    local op="$1";      shift
    case "$op" in
        -h|--help)      ___x_cmd help -m cron "$@";    return 0 ;;

        *)              N=cron M="Unknown subcmd -> $op" log:ret:64 ;;
    esac
}
