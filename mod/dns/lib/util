# shellcheck shell=dash

___x_cmd_dns_util___linux_pick_backend_(){
    x_=file
    ___x_cmd hascmd systemctl || return 0

    if systemctl is-active --quiet NetworkManager && ___x_cmd hascmd nmcli; then
        x_=nmcli
    elif systemctl is-active --quiet systemd-resolved; then
        x_=systemd
    fi 2>/dev/null
}
