# shellcheck shell=dash

xrc:mod:lib     user        add ls current

___x_cmd_user___main(){
    [ "$#" -gt 0 ] || {
        ___x_cmd_user_current
        return
    }

    local op="$1"; shift
    case "$op" in
        add|ls|rm|current)
            ___x_cmd_user_"$op" "$@" ;;
        -h|--help) x help -m user >&2; return 1 ;;
    esac
}
