# shellcheck shell=dash

xrc:mod:lib     set     xready

___x_cmd_set___main(){
    local op="$1"; shift
    case "$op" in
        xready)
            ___x_cmd_set_"$op" "$@"
            ;;
    esac
}
