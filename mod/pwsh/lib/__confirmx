___x_cmd_pwsh___confirmx(){
    local cmd="$1"
    if x yesno "Are you sure to execute cmd -> $cmd"; then
        pwsh:info "Execute cmd -> $cmd"
        x pwsh <<A
$cmd
A
    else
        pwsh:info "Abort"
        return 1
    fi
}
