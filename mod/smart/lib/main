# shellcheck shell=dash
___x_cmd_smart___main(){
    [ "$#" -gt 0 ] || {
        x smartctl --help
        return
    }

    local op="$1"; shift
    case "$op" in
        *) x smartctl "$op" $@ ;;
    esac
}
