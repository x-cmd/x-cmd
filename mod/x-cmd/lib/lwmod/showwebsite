
___x_cmd_showwebsite(){
    local website="$1"
    local msg="${2:-For more info}"
    ___x_cmd open "$website" || {
        x:info "$msg  ->  $website"
        ___x_cmd qrencode "$website" || true
    }
}

