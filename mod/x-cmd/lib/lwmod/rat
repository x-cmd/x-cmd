
___x_cmd_rat(){
    local IFS=
    local arg; local line; for arg in "$@"; do
        [ -f "$arg" ] || {
            x:error "Unexisted file=${arg}"
            return 64
        }

        while read -r line; do
            printf "%s\n" "$line"
        done <"$arg"
    done
}
