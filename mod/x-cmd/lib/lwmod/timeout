

# TODO: We already have a more powerful timeout module to handle busybox situation.

___x_cmd_timeout(){
    if ___x_cmd_hascmd timeout; then
        ___x_cmd_cmds timeout "$@"
    else
        ___x_cmd cosmo timeout "$@"
    fi
}
