# shellcheck shell=sh
# shellcheck disable=

___x_cmd_gh_repo_topics(){
    :
}

# https://docs.github.com/en/rest/repos/repos#get-all-repository-topics
___x_cmd_gh_repo_topics_ls(){
    :
}

# x gh repo topics replace -r xxx/xxx aaa,bbb,ccc
# empty is clean all topics. and will replace that need confirm
# https://docs.github.com/en/rest/repos/repos#replace-all-repository-topics
___x_cmd_gh_repo_topics_replace(){
    :
}
