# shellcheck shell=dash

___x_cmd_apt___install(){
    local X_help_cmd='x help -m apt install'
    help:arg:parse

    if [ $# -eq 0 ]; then
        ___x_cmd_apt___nv
        return 0
    fi

    # Using x sudo
    x sudo apt install "$@"
}
