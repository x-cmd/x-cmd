# shellcheck shell=dash

___x_cmd_luarocks_install(){
    (
        # TODO: Can't find gcc,cc,make etc
        if ! ___x_cmd_hascmd gcc; then
            x lua ensurecc || return 1
        fi

        ___x_cmd_luarocks_exec install "$@"
    )
}
