# shellcheck shell=dash

xrc:mod:lib clawhub              \
            skill/search          \
            skill/get             \
            skill/versions        \
            skill/scan            \
            skill/file            \
            skill/resolve         \
            skill/download        \
            skill/publish         \
            skill/transfer        \
            skill/moderate

___x_cmd_clawhub_skill(){
    local X_help_cmd='___x_cmd_clawhub___help skill'
    help:arg:parse

    local op="$1"
    case "$op" in
        search|ls|get|versions|version|scan|file|resolve|download|publish|delete|undelete|rename|merge|moderate) shift
            "___x_cmd_clawhub_skill_$op" "$@"                         ;;
        transfer) shift
            ___x_cmd_clawhub_skill_transfer "$@" ;;
        "") ___x_cmd_clawhub_u_subcmd_invalid skill "" ;;
        *) ___x_cmd_clawhub_u_subcmd_invalid skill "$@" ;;
    esac
}
