
# x tui form \
#   id   "Id for repo" x-cmd/x-cmd  x-cmd/release =~ '[A-Za-z0-9][A-Za-z0-9_]+' --
#   name "name for repo" x-cmd/x-cmd  x-cmd/release =~ '[A-Za-z0-9][A-Za-z0-9_]+'

___x_cmd_tui_form(){
    :

    # varaible-a, description, current-value(might be wrong), default-value(must be correct, or ""), regex/rule using param style
    # varaible-b, description, current-value(might be wrong), default-value(must be correct, or ""), regex/rule using param style
    # varaible-c, description, current-value(might be wrong), default-value(must be correct, or ""), regex/rule using param style

}
