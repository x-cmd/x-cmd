
name: python

provider:
  default: gemini

  gemini:
    model:  gemini-pro

  openai: # special parameters for openai
    seed:
    model: gpt-3.5-turbo
    temperature:

prompt:
  system:

  context:
  example:
  content: '%{BODY}%'

history:
