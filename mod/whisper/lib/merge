
___x_cmd_whisper_merge(){
    local srtfile="$1"
    local input="$2"
    local output="$3"

    # x ffmpeg -i "$file" -vf subtitles="$srtfile" "$output"
    # TODO: A special module for this.
    x ffmpeg -i "$input" -lavfi "subtitles=$srtfile:force_style='Alignment=2,MarginV=5'" "$output"
}
