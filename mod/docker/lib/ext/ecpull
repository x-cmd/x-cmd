
___x_cmd_docker_ecpull(){
    local image="$1"

    case "$image" in
        */*:*)      ;;
        *:*)        image="library/${image}" ;;
        *)          image="library/${image}:latest"
    esac

    ___x_cmd docker pull "public.ecr.aws/docker/${image#/}"
}

