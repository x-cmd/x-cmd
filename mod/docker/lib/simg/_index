
# snap image ...

___x_cmd_docker_simg(){
    [ $# -gt 0 ]    ||      set -- --help

    local op="$1";  shift

    case "$op" in
        -h|--help)      ___x_cmd help -m docker simg    "$@" ;;

        download|prepare)
                        ___x_cmd_docker_simg_"$op"      "$@" ;;
    esac
}
