# shellcheck shell=dash
# generate shell command

___x_cmd_coco_exec(){
    local LOCALSHELL=
    local OS=

    local x_=""
    x os name_

    LOCALSHELL="$___X_CMD_SHELL" OS="$x_" x chat --send --type coco,shell --minion "$___X_CMD_ROOT_MOD/coco/lib/minion/exec.yml" "$@"
}

