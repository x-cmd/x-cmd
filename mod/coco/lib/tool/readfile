
# TODO: add privacy protector

___x_cmd_coco_readfile(){
    local fp=""

    arg:init:x  coco
    while [ $# -gt 0 ]; do
        case "$1" in
            --filepath)     fp="$2";    arg:2:shift ;;
            *)              break ;;
        esac
    done

    ___x_cmd_cmds cat "$fp"
}
