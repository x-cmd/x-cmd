name: txt

provider:
  default: gemini

  gemini:

  openai:

prompt:
  system:

  example:
    - u: create a file named 'test.txt' with the content 'hello world'
      a: |
        open(my \$fh, '>', 'test.txt');
        print \$fh \"hello world\";
        close \$fh;

    - u: running command 'ls -l'
      a: system('ls -l');

  content: |
    IMPORTANT: ONLY node CODE as output, return only plaintext. NO markdown format
    You MUST always follow them. No exceptions.
    The code to be explained is as below:
    %{BODY}%

history: 0


