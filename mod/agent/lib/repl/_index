

___x_cmd_agent_repl(){

    local msgfile="$1"

    {
        ___x_cmd tail -n 9 -f "$msgfile"
    } | {
        local msg
        while read -r msg; do
            case "$msg" in
                "")     ;;

            esac
        done
    }
}
