# shellcheck shell=dash

xrc:mod:lib     agent       setup tdown util unify classify cfg/_index browser request/_index job/_index chat/_index session/_index stat/_index

___x_cmd_agent___main(){
    [ "$#" -gt 0 ] ||       set -- --help

    local op="$1";          shift
    case "$op" in
        -h|--help)          ___x_cmd help -m agent      "$@" ;      return 0 ;;

        webfetch)           ___x_cmd_agent_webfetch     "$@" ;;
        # browse|browser)     ___x_cmd ab                 "$@" ;;
        browse|browser)     ___x_cmd_agent_browser      "$@" ;;


        --setup|setup)      ___x_cmd_agent_setup        "$@" ;;
        tdown|teardown)     ___x_cmd_agent_tdown        "$@" ;;
        --is-setup)         ___x_cmd_agent_is_setup     "$@" ;;
        --should-auto-setup)
                            ___x_cmd_agent_should_auto_setup "$@" ;;
        --cfg|--cur|init)   ___x_cmd_agent_"${op#--}"   "$@" ;;

        skill)              ___x_cmd skill              "$@" ;;

        run)                ___x_cmd_agent_job_run      "$@" ;;
        ps)                 ___x_cmd_agent_job_ps       "$@" ;;

        request|chat|job|session|stat|classify|\
        unify|isready|default_harness|default_harness_)
                            ___x_cmd_agent_"$op"        "$@" ;;

        *)                  N=agent M="Unknown subcmd -> $op" log:ret:64 ;;
    esac
}
