# shellcheck shell=dash
___x_cmd_agent___main(){
    [ "$#" -gt 0 ] || {

        return
    }

    local op="$1"; shift
    case "$op" in
        --help|-h|*) x advise gen_help "$___X_CMD_ROOT_MOD/agent/res/advise.jso" "$@" ;;
    esac
}
