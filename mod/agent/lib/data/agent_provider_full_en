claude                  # Anthropic Claude Code (OAuth login, uses ~/.claude/settings.json)
claude-openrouter       # Claude Code + OpenRouter backend (API key)
claude-deepseek         # Claude Code + DeepSeek backend (API key)
claude-kimi             # Claude Code + Moonshot Kimi backend (API key)
claude-moonshot         # Claude Code + Moonshot AI backend (API key)
claude-zhipu            # Claude Code + Zhipu GLM backend (API key)
claude-siliconflow      # Claude Code + SiliconFlow backend (API key)
claude-doubao           # Claude Code + Doubao backend (API key)
claude-mthreads         # Claude Code + MThreads backend (API key)
claude-minimax          # Claude Code + MiniMax backend (API key)
claude-ali              # Claude Code + Alibaba Cloud Bailian backend (API key)
claude-baidu            # Claude Code + Baidu Qianfan backend (API key)
claude-tencent          # Claude Code + Tencent Cloud backend (API key)
codex                   # OpenAI Codex CLI (OAuth / device code)
codex-ds                # OpenAI Codex + DeepSeek model
cursor                  # Cursor Agent (Cursor login)
gemini-cli              # Google Gemini CLI (API key)
opencode                # Opencode (OAuth / API key)
kimi-cli                # Kimi Code CLI (login / API key)
x-chat                  # x-cmd chat (API key, dispatches to providers via x chat)
