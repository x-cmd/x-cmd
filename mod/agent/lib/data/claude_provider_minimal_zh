claude                  # Anthropic 官方（OAuth 登录，使用 ~/.claude/settings.json）
openrouter              # OpenRouter（API key）
deepseek                # DeepSeek（API key）
kimi                    # Moonshot Kimi（API key）
moonshot                # Moonshot AI（API key）
zhipu                   # 智谱 GLM（API key）
siliconflow             # 硅基流动（API key）
doubao                  # 豆包（API key）
mthreads                # 摩尔线程（API key）
minimax                 # MiniMax（API key）
ali                     # 阿里云百炼（API key）
baidu                   # 百度千帆（API key）
tencent                 # 腾讯云（API key）
More                    # 显示全部可用 AI 工具（Codex、Cursor、Gemini 等）
Skip for now            # 跳过设置，之后可随时运行 `x agent init`
