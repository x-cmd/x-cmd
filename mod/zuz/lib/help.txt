SYMPOSIS:
    x z   <zipfile> <source...>
    x uz  <zipfile>
    x zuz uzr <zipfile>
    x zuz ls  <zipfile>

subcommand:
    z|compress          compress file
    uz|decompress       decompress file
    uzr                 decompress file then remove the original file
    ls                  list compress inside file
