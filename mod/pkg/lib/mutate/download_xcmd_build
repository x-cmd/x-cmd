# shellcheck    shell=sh            disable=SC2016,SC3043      # xrc


___x_cmd_pkg_download_xcmd_build(){
    case "${___X_CMD_WEBSRC_REGION}" in
        cn)     ___x_cmd_pkg_download_xcmd_build_cn ;;
        *)      ___x_cmd_pkg_download_xcmd_build_internet ;;
    esac
}

___x_cmd_pkg_download_xcmd_build_internet(){
    :
    # Using github release

    # Using github pkg release

    # Using docker hub release      # 200 Times

}

___x_cmd_pkg_download_xcmd_build_cn(){
    :
    # Using docker hub
    # 100-300KB/s More or less.

    # Using gitcode pkg release

    # A script to help organize the release.
    # Then we make use of tuna to update it.
    # Using a very simple iterate script.

    # Tuna release folder   # No such thing.    # Using git to sync # 100GB
}

