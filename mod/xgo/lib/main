# shellcheck shell=dash

xrc:mod:lib     xgo   __runcmd

___x_cmd_xgo___main(){
    [ "$#" -gt 0 ] ||   set -- --help

    local op="$1";      shift
    case "$op" in
        -h|--help)      ___x_cmd help -m xgo "$@";    return 0  ;;

        --|--runmain)   ___x_cmd_xgo___runmain         "$@"    ;;
        *)              ___x_cmd_xgo___runmain "$op"   "$@"    ;;
    esac
}
