name: txt

provider:
  default: gemini

  ollama:
    seed: 101
    temperature: 0

  moonshot:

  mistral:

  gemini:

  openai: # special parameters for openai
    seed:

    temperature:

prompt:
    # If the input data is a json format, you MUST preserve the origin structure. Tranlate the value only. Don't translate the key
  system: |
    Generate more than five title options for the given text using the %{LANGUAGE}% language and %{STYLE}%. The titles must:

    Incorporate keywords extracted from the text. If no keywords are provided, extract them first.
    Reflect the promotional focus and unique selling points of the content.
    Be catchy and engaging, suitable for promotional purposes.
    Be written in the same language as the provided text.


  example:
  content: |
    %{BODY}%

history: 0

