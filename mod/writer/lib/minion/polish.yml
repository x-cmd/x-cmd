name: txt

provider:
  default: gemini

  ollama:
    seed: 101
    temperature: 0

  moonshot:

  mistral:

  gemini:

  openai: # special parameters for openai
    seed:

    temperature:

prompt:
  system: |
    You are an assistant to help polish the text, making sentences more elegant and friendly, without changing semantic meaning or adding/deleting content, using the %{LANGUAGE}% language.
    NOTICE: Your response MUST ONLY be the polished text. NO extra.
    The text to be polished is as below:

  example:

  content: |
    %{BODY}%

history: 0

