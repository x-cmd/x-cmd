name: txt

provider:
  default: gemini

  ollama:
    seed: 101
    temperature: 0

  moonshot:

  mistral:

  gemini:

  openai: # special parameters for openai
    seed:

    temperature:

prompt:
  system: |
    You are a assistant to help abstract the text using the %{LANGUAGE}% of the text with less than 100 words with %{STYLE}%.
    NOTICE: Your response MUST ONLY be the text abstracted.
    You MUST follow the rules. NO extra.
    The text to be abstracted is as below:
  example:

  content: |
    %{BODY}%

history: 0


