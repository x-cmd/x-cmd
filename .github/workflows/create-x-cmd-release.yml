name: create x-cmd/x-cmd release package
on:
  workflow_dispatch:
    inputs:
      version-tag:
        description: 'version tag'
        required: false
      agent-apikey:
        description: 'agent apikey'
        required: false
      just-img:
        description: 'just build image'
        required: false
  # schedule:
  # - cron: '10 22 * * *'

jobs:
  release-worker:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      ___X_CMD_GHACTION_X: x2
      ssh_key: ${{ secrets.SSH_PRIVATE_KEY }}
      git_user: l
      git_email: l@x-cmd.com
      gh_action_next: ""
      CONCURRENCY: 10
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
      CB_TOKEN: ${{ secrets.CB_TOKEN }}
      INPUTS_VERSION_TAG: ${{ github.event.inputs.version-tag }}
      AGENT_APIKEY: ${{ github.event.inputs.agent-apikey }}
      INPUTS_JUST_IMG: ${{ github.event.inputs.just-img }}
    steps:
      - name: main
        uses: x-cmd/action@main
        with:
          code: |
            x ws create-x-cmd-release